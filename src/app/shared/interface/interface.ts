export interface ColumnObject {
    columnId: string;
    propertyName: string;
}

export interface ImageUrls {
    imagePath?: string[];
}

export interface MessageCount {
    messageNum?: number[];
    userName?: string;
}